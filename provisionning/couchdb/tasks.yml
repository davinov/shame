---
- name: CouchDB | Install
  apt: pkg=$item state=latest update_cache=yes
  with_items:
  - couchdb
  notify:
  - couchdb-restart
  - apache-reload

- name: CouchDB | Configure
  copy: src=couchdb/config.ini dest=/etc/couchdb/local.ini
  notify:
  - couchdb-restart
