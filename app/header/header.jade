a.title(href="#") Shame
ul.actions.navbar-right
  li
    a.login(href="auth/facebook", ng-if="!user") Login with Facebook
    a.user(href="#", ng-if="user")
      img.photo(ng-src="{{user.photo}}", ng-if="user.photo")
      | {{ user.displayName }}
